#include <bits/stdc++.h>

using namespace std;
#define endl '\n';

int main(void) {
    int n; cin >> n;
    string a, b, c;
    while(n--) {
        cin >> a >> b >> c;
        cout << a[0] << b[0] << c[0] << endl;
    }
}
